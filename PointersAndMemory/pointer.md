# Definition

A pointer is a variable that stores the memory address of another variable rather than it's actual value.

## Two operators associated with pointers

- **The Address-of operator(&)** Gives you the memory address where a variable is stored
- **The Dereference operator(*)** Lets you access or modify the value stored at that memory address
  